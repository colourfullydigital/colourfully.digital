/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  transpilePackages: ["@colourfully-digital/shared"],
  // Add any other Next.js configuration options here
}

module.exports = nextConfig
