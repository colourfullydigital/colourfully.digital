# macOS
.DS_Store
.AppleDouble
.LSOverride

# Icon must end with two carriage returns
# Icon


# Thumbnails
.Thumbs.db

# Server-side macOS metadata
.Spotlight-V100
.Trashes

# Files that might appear on external disks
ehthumbs.db
Desktop.ini

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
# lockfiles (choose which to commit; common to ignore here)
package-lock.json
pnpm-lock.yaml
yarn.lock

# Build outputs
.output/
.next/
dist/
build/
public/build/
/.astro/

# Astro & Vite
/.cache
/.vite
/.vercel
/.vercel_build_output

# TypeScript
*.tsbuildinfo

# Declaration files (if generated into repo)
*.d.ts

# Environment files
.env
.env.*.local
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
/logs
*.log
npm-debug.log*

# Local runtime data
pids
*.pid
*.seed
*.pid.lock

# Editor directories and files
.vscode/
.history/
.idea/
*.sublime-workspace
*.sublime-project

# VS Code test extensions
.vscode-test/

# Coverage
coverage/
.nyc_output/

# Test output
/coverage
/jest-fixtures

# Optional npm cache directory
.npm

# Storybook build outputs
/storybook-static/

# Misc
*.tgz
*.gz
*.zip

# Local dev server files
/.parcel-cache

# Sanity.io (local datastore, caches, exports, and studio build artifacts)
# Keep config files like `sanity.json` or `sanity.config.*` in source control.
.sanity/
sanity.lock
*.ndjson
*.ndjson.gz
sanity-export-*.tar.gz

# If you place a Sanity Studio in a subfolder (commonly `studio/`), ignore its
# local cache and build outputs but not its config files.
studio/.sanity/
studio/.sanity-cache/
studio/.cache/
studio/dist/
studio/node_modules/


